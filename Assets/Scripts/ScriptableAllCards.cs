using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
using TMPro;

[CreateAssetMenu(fileName = "SOAllCards", menuName = "ScriptableObjects/SOAllCards")]
public class ScriptableAllCards : ScriptableObject
{
    public ScriptablePlayerCard[] scriptableObjectIndividualCard;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
